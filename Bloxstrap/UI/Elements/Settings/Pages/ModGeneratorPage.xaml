<ui:UiPage
    x:Class="Bloxstrap.UI.Elements.Settings.Pages.ModGeneratorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dmodels="clr-namespace:Bloxstrap.UI.ViewModels.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Bloxstrap.UI.ViewModels"
    xmlns:resources="clr-namespace:Bloxstrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="ModsPage"
    d:DesignHeight="1500"
    d:DesignWidth="800"
    Scrollable="True"
    mc:Ignorable="d">

    <StackPanel Margin="0,0,14,14">

        <Grid Margin="0,16,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:CardAction Grid.Row="0" Grid.Column="0" Margin="0,0,4,0" Command="{Binding OpenCommunityModsCommand}" Icon="BuildingShop20">
                <StackPanel>
                    <TextBlock FontSize="14" Text="Community Mods" />
                    <TextBlock  Margin="0,2,0,0" Padding="0,0,16,0" FontSize="12" Foreground="{StaticResource TextFillColorTertiaryBrush}" Text="Download mods made by other people." TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>

            <ui:CardAction Grid.Row="0" Grid.Column="1" Margin="4,0,0,0" Command="{Binding OpenPresetModsCommand}" Icon="ReadingModeMobile24">
                <StackPanel>
                    <TextBlock FontSize="14" Text="Preset Mods" TextWrapping="Wrap" />
                    <TextBlock Margin="0,2,0,0" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Use our built-in mods." TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>

            <ui:CardAction Grid.Row="1" Grid.ColumnSpan="2" Margin="0,7,0,0" VerticalAlignment="Top" Command="{Binding OpenModsCommand}" Icon="List24">
                <StackPanel>
                    <TextBlock FontSize="14" Text="My Mods" TextWrapping="Wrap" />
                    <TextBlock Margin="0,2,0,0" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Change the applying order of mods." TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>
        </Grid>

        <Separator Margin="0,8" />

        <Grid Margin="8,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Margin="0,0,0,4">

                <!-- Glyph Preview -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,8">
                    <TextBlock
                                Margin="0,0,12,0"
                                FontWeight="Bold"
                                FontSize="20"
                                Text="Font Preview" />

                    <ComboBox 
                                Width="200" 
                                ItemsSource="{Binding FontDisplayNames}"
                                SelectedItem="{Binding SelectedFontDisplayName}"/>
                </StackPanel>

                <ScrollViewer Height="260" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding GlyphItems}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="52" Height="52" Margin="4"
                                            Padding="4"
                                            Background="{DynamicResource ControlFillColorSecondaryBrush}"
                                            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                            BorderThickness="1"
                                            CornerRadius="6">
                                    <Viewbox Stretch="Uniform">
                                        <Path Data="{Binding Data}" Margin="0,0,10,8" Fill="{Binding ColorBrush}"/>
                                    </Viewbox>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <StackPanel
                            Margin="0,8"
                            HorizontalAlignment="Left"
                            Orientation="Horizontal">
                    <Button
                                Command="{Binding GenerateModCommand}"
                                IsEnabled="{Binding IsNotGeneratingMod}"
                                Content="{x:Static resources:Strings.Menu_Mods_ModGenerator_QuickActions_GenerateMod}" />

                    <CheckBox
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                IsChecked="{Binding IncludeModifications}"
                                Content="{x:Static resources:Strings.Menu_Mods_ModGenerator_QuickActions_CopyToModsFolder}" />

                    <TextBlock
                                Margin="8,0"
                                VerticalAlignment="Center"
                                Text="Solid Color:" />

                    <TextBox
                                Width="80"
                                Margin="0,0,8,0"
                                Text="{Binding SolidColorHex, UpdateSourceTrigger=PropertyChanged}"
                                VerticalContentAlignment="Center" />

                    <ui:Button
                                Command="{Binding OpenColorPickerCommand}"
                                Content="{x:Static resources:Strings.Common_Color}" />

                </StackPanel>

                <StackPanel Margin="0,0,0,8" Orientation="Horizontal">
                    <CheckBox
                                Margin="4,0"
                                IsChecked="{Binding ColorCursors}"
                                Content="{x:Static resources:Strings.Menu_Mods_ModGenerator_GenerationOptions_Cursor}" />
                    <CheckBox
                                Margin="4,0"
                                IsChecked="{Binding ColorShiftlock}"
                                Content="{x:Static resources:Strings.Menu_Mods_ModGenerator_GenerationOptions_Shiftlock}" />
                    <CheckBox
                                Margin="4,0"
                                IsChecked="{Binding ColorEmoteWheel}"
                                Content="{x:Static resources:Strings.Menu_Mods_ModGenerator_GenerationOptions_EmoteWheel}" />
                </StackPanel>

                <TextBlock
                            Margin="0,6,0,0"
                            FontWeight="Bold"
                            TextWrapping="Wrap"
                            Text="{Binding StatusText}" />
            </StackPanel>
        </Grid>
    </StackPanel>
</ui:UiPage>
